{
    "name": "ll-analyzer-root",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "ll-analyzer"
    ],
    "scripts": {
        "build": "node build-standalone.mjs",
        "build:standalone": "node build-standalone.mjs",
        "start": "node build-standalone.mjs",
        "dev": "npm run dev --workspace=ll-analyzer",
        "lint": "npm run lint --workspace=ll-analyzer"
    }
}